import { css } from "styled-components"

export default css`
  .underline {
    text-decoration: underline;
  }

  .ellipsis {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .bold {
    font-weight: 500;
  }
`
